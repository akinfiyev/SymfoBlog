<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('default') }}">
            <img src="{{ asset('images/logo.png') }}" width="30" height="30" alt="">
            SymfoBlog
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav mr-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('default') }}">Home</a></li>
                {% if is_granted('ROLE_BLOGGER') %}
                    <li class="nav-item{% if app.request.attributes.get('_route') == 'articles_add' %} active{% endif %}"><a class="nav-link" href="{{ path('articles_add') }}">Add article</a></li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item"><a class="nav-link" href="{{ path('app_logout') }}">Logout</a></li>
                {% else %}
                    <li class="nav-item{% if app.request.attributes.get('_route') == 'registration' %} active{% endif %}">
                        <a class="nav-link" href="{{ path('user_registration') }}">Registration</a></li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>